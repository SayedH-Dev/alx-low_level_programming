#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
  * _atoi - function that turns string to integer
  * @s: string to be turned into int
  * Return: result int
  */
int _atoi(char *s)
{
	return (atoi(s));
}
