#include "main.h"

/**
  * add - function to add 2 integers
  * @x: first no. to add
  * @y: second number to add
  * Return: total
  */
int add(int x, int y)
{
	int total;

	total = x + y;

	return (total);
}
